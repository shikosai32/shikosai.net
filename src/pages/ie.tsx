import type { NextPage } from "next";
import Layout from "components/Layout";

const Ie: NextPage = () => (
  <Layout PageTitle="Don't Use IE">
    <div>
      <h1>IEEEEEE</h1>
    </div>
  </Layout>
);

export default Ie;
